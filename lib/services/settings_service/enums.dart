enum MapProvider {
  openStreetMap,
  apple,
}

enum GeocoderProvider {
  system,
  geocodeMapsCo,
  nominatim,
}

enum AndroidTheme {
  materialYou,
  miui,
}

enum HelperSheet {
  radiusBasedAlarms,
  taskShare,
}
